/**
 * Mock Database - Main export
 * 
 * This provides a database-like structure for mock data.
 * Tables contain flat data (like SQL rows).
 * Queries provide relationship helpers (like SQL JOINs).
 */

// Tables (raw data)
export * from './tables';

// Queries (relationship helpers)
export * from './queries';
